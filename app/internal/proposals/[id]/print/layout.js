// Print layout: NO incluir el chrome del internal hub.
// Este layout es minimal y permite que ProposalDocument tome toda la viewport.
export default function PrintLayout({ children }) {
  return children
}
